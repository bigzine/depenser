# Dépendances de développement / notebooks — PAS utilisées dans l'image Docker de l'API.
# Installer avec : pip install -r requirements-dev.txt -r requirements.txt
#
# Garder requirements.txt (API) minimal et séparé permet une image Docker plus légère
# et plus rapide à construire pour le déploiement.

-r requirements.txt

mlflow==3.15.0
optuna==4.9.0
scikit-learn==1.5.2
xgboost==2.1.1
matplotlib==3.9.2
seaborn==0.13.2
pytest==8.3.3
httpx==0.27.2
streamlit==1.39.0
evidently==0.7.21
